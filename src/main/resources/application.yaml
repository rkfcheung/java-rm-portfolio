spring:
  application:
    name: portfolio-dashboard

management:
  endpoint:
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: health

portfolio:
  input: classpath:sample-input.csv
